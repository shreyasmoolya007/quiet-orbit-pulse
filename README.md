# Quiet Orbit

A small scheduled signal runner. Target configuration, recipient information, and detailed results remain in encrypted secrets or private source.

The public status surface intentionally exposes only a generic match/no-match state.
